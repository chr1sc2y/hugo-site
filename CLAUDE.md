# Blog maintenance

Read and follow [AGENTS.md](AGENTS.md). It is the canonical guide for this repository's English-only content policy, Hugo workflow, and GitHub Pages deployment model.

The generated `public/` directory is for local verification only and must not be committed. Push source changes to `master`; GitHub Actions owns the `gh-pages` branch.
